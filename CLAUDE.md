# xyzzy を触るときの原則

手順や環境の話は書かない。**変わらないことだけ**をここに置く。

- ビルドと検証の手順、踏みやすい罠 … `docs/development.md`
- 画面を使わない検証台 … `misc/verify/README.md`
- Lisp から見える非互換の記録 … `docs/incompatible-changes.md`

## 後方互換は原則、破るなら記録する

Lisp の動作は 0.2.2.235 と互換であることを原則とする。現代化のために緩めるときは、
リポジトリ内の Lisp と整合が取れていること、**利用者の Lisp 資産に影響する変更は
その内容と移行方法を `docs/incompatible-changes.md` に書くこと**、の両方を満たす。
（詳しくは `README.md` の「xyzzy 0.2.2 系列とは」）

## 広く型を変えたら、全部作り直して確かめる

増分ビルドは**前回エラーで終わったファイルを飛ばし、「0 エラー」と表示する**。
移行のように広い範囲へ手を入れた後は、必ず全ビルドで確かめる。

型検査に頼るときは、指されない抜け道が三つあることを覚えておく。
**整数に載せた文字列**（`LPARAM` に渡す `char*` は通ってしまう）、
**器の大きさ**（広げた型からコピーする先が溢れても黙っている）、
**誰も呼んでいない関数**（リポジトリ内に呼び出しもテストも無いものは、
移行したつもりで取り残される）。

## Lisp を変えたらバイトコンパイルしてから確かめる

xyzzy は `.lc` があればそちらを読む。`.l` だけ直して動かすと、**古い定義のまま
動いて混乱する**。組み込み関数を消す・改名するときは、先に `.lc` を消す。

組み込み関数を足したら、`lisp/builtin.l` にも宣言を足す。

## 検証で利用者の画面を奪わない

確かめるために xyzzy を起動するときは `misc/verify/` の仕掛けを使い、
別のデスクトップで走らせる。画面を実際に使う必要があるものは、
**始める前に利用者へ都合を聞く**。

## 外が ANSI で確定している相手は、そのまま残すのが正しい

第三者製の書庫 DLL、DDE、文字集合ごとのフォント見本、winhelp の索引などは、
相手側の形式が CP932 やバイト列で決まっている。**W 系へ移し忘れているのではなく、
意図してそこに置いてある。** 触る前にその場のコメントを読む。
